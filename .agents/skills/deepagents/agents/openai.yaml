interface:
  display_name: "Deepagents"
  short_description: "Repo-specific patterns and workflows for deepagents"
  default_prompt: "Use the deepagents repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true