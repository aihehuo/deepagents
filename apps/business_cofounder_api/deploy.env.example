# Copy to .deploy.env (DO NOT COMMIT .deploy.env) and fill values.
#
# This file is for deployment/push configuration (registry/username/hosts).
# Keep real credentials and internal hostnames ONLY in .deploy.env (ignored by git).

# ---- Registry
# Example: crpi-xxxxxx.cn-qingdao.personal.cr.aliyuncs.com
ALIYUN_DOCKER_REGISTRY=

# Example: your-email@example.com
ALIYUN_DOCKER_USERNAME=

# Optional: image repo/name inside the registry (defaults to aihehuo/business-cofounder-api)
DOCKER_IMAGE_NAME=aihehuo/business-cofounder-api

# Optional: default tag (defaults to latest for pull/deploy; 0.0.1 for build_and_push)
DOCKER_IMAGE_TAG=latest

# ---- Production host defaults (optional; can still be passed via env when running script)
REMOTE_HOST=
REMOTE_USER=root
REMOTE_DIR=/mnt/business-cofounder-api
CONTAINER_NAME=business-cofounder-api
PORT=8001


