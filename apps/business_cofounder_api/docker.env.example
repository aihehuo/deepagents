# Copy to .docker.env and fill values.

# ---- Model/provider selection
# Switch between DeepSeek (Anthropic-compatible) and Qwen (OpenAI-compatible).
# Supported values: deepseek | qwen
MODEL_API_PROVIDER=deepseek

# ---- Model settings (generic)
# Provider-specific defaults will be used if MODEL_NAME is unset:
# - deepseek -> deepseek-chat
# - qwen     -> qwen-plus
MODEL_NAME=deepseek-chat
MODEL_API_MAX_TOKENS=20000
MODEL_API_TEMPERATURE=0.2
MODEL_API_TIMEOUT_S=180

# ---- API endpoint + key (generic)
# DeepSeek (Anthropic-compatible proxies) often require a base URL; Qwen compatible-mode also requires base URL.
MODEL_BASE_URL=
MODEL_API_KEY=

# ---- Coder subagent model (optional; defaults to MODEL_* if unset)
# If you want the agent to delegate code/HTML tasks to a different model, set CODER_MODEL_*.
CODER_MODEL_API_PROVIDER=qwen
CODER_MODEL_BASE_URL=
CODER_MODEL_API_KEY=
CODER_MODEL_NAME=qwen-coder

# ---- Logging
BC_API_LOG_CHAT_IO=1
BC_API_LOG_TRUNCATE_CHARS=2000


